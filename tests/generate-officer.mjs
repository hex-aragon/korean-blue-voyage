import {chromium} from '@playwright/test';
import fs from 'node:fs/promises';
const browser=await chromium.launch({channel:'chrome',headless:true});const page=await browser.newPage();await page.route('**/officer-studio',r=>r.fulfill({contentType:'text/html',body:'<!doctype html><html><body></body></html>'}));await page.goto('http://127.0.0.1:5197/officer-studio');
const data=await page.evaluate(async()=>{const T=await import('/node_modules/three/build/three.module.js');const {makeOfficer}=await import('/src/officer.js');const {RoomEnvironment}=await import('/node_modules/three/examples/jsm/environments/RoomEnvironment.js');const r=new T.WebGLRenderer({alpha:true,antialias:true,preserveDrawingBuffer:true});r.setSize(640,800);r.toneMapping=T.ACESFilmicToneMapping;r.toneMappingExposure=1.15;const s=new T.Scene(),env=new T.PMREMGenerator(r),room=new RoomEnvironment();s.environment=env.fromScene(room).texture;s.environmentIntensity=.5;s.add(new T.HemisphereLight(0xc6ddea,0x343b44,2));const key=new T.DirectionalLight(0xffdfba,3);key.position.set(-2,3,-4);s.add(key);const rim=new T.DirectionalLight(0x8dcaf1,3);rim.position.set(2,2,1);s.add(rim);const officer=makeOfficer();s.add(officer);const camera=new T.PerspectiveCamera(28,.8,.01,20);camera.position.set(-.47,1.52,-2.25);camera.lookAt(0,1.25,0);r.render(s,camera);return r.domElement.toDataURL('image/webp',.95);});await fs.writeFile('public/officer.webp',Buffer.from(data.split(',')[1],'base64'));await browser.close();
