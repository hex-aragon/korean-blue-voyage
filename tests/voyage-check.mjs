import {chromium,expect} from '@playwright/test';
const browser=await chromium.launch({channel:'chrome',headless:true});const page=await browser.newPage({viewport:{width:1280,height:900}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('http://127.0.0.1:5197');await page.locator('#start').click();await page.locator('[data-panel="fleet"]').click();
for(const id of ['tanker','bulk','chemical','yacht']){await page.locator(`[data-ship="${id}"]`).click();await expect(page.locator(`[data-ship="${id}"]`)).toHaveClass(/chosen/);}
await page.locator('#close-panel').click();await page.locator('[data-panel="jobs"]').click();await page.locator('[data-job="0"]').click();await page.locator('#job-complete').click();await expect(page.locator('#credits')).toHaveText('₩ 0');await page.locator('#job-auto').click();await page.locator('#time-speed').click();await page.locator('#time-speed').click();await expect(page.locator('#credits')).toHaveText('₩ 1,200',{timeout:90000});await page.reload();await expect(page.locator('#credits')).toHaveText('₩ 1,200');await page.locator('#start').click();await page.locator('[data-panel="chart"]').click();await page.locator('[data-region="han"]').click();await page.locator('#close-panel').click();await page.locator('[data-panel="fleet"]').click();await page.locator('[data-ship="tanker"]').click();await expect(page.locator('[data-ship="yacht"]')).toHaveClass(/chosen/);await page.locator('#close-panel').click();await page.locator('#photo').click();await page.screenshot({path:'/tmp/yoonseul-final-river.png'});console.log(JSON.stringify({delivery:'passed',persistence:'passed',allShips:'passed',riverDraftLimit:'passed',errors}));await browser.close();if(errors.length)process.exit(1);
