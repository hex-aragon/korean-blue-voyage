import {chromium,expect} from '@playwright/test';
const browser=await chromium.launch({channel:'chrome',headless:true}),page=await browser.newPage({viewport:{width:1440,height:960}}),errors=[];page.on('pageerror',e=>errors.push(e.message));
await page.addInitScript(()=>{if(!sessionStorage.getItem('fixture')){localStorage.setItem('yoonseul-save',JSON.stringify({mode:'mission',ship:'container',region:'busan',training:{run:{phase:2,status:'practice',grade:4,region:'busan',ship:'container',firstTry:2,actions:2,startDeliveries:0,history:[],review:[]}},trip:{version:1,region:'busan',ship:'container',position:{x:0,z:-300,heading:0},cargo:{bays:[1,1,1,1,1,1],high:false,ballast:false}}}));sessionStorage.setItem('fixture','1');}});
await page.goto(process.env.VOYAGE_URL||'http://127.0.0.1:5198/?trafficSeed=42');await page.locator('#start').click();await expect(page.locator('#ocean canvas')).toHaveAttribute('data-model','blender',{timeout:30000});await page.screenshot({path:'/tmp/blender-final-ship.png'});await page.locator('#throttle').fill('20');await page.locator('#throttle').dispatchEvent('input');for(let i=0;i<3;i++)await page.locator('#steering-wheel').press('ArrowRight');await page.waitForTimeout(2000);await page.locator('#center-wheel').click();await expect.poll(()=>page.evaluate(()=>JSON.parse(localStorage.getItem('yoonseul-save')).training.run.phase),{timeout:12000}).toBe(3);await page.locator('#camera-toggle').click();await page.screenshot({path:'/tmp/blender-final-bridge.png'});await page.setViewportSize({width:390,height:844});await page.locator('#training-chip').click();await expect(page.locator('.training-current')).toBeInViewport();await page.screenshot({path:'/tmp/training-final-mobile.png'});expect(await page.evaluate(()=>document.documentElement.scrollWidth)).toBe(390);console.log({model:'loaded Blender GLB',helm:'10 degree hold and center accepted',mobile:'current step in view',errors});await browser.close();expect(errors).toEqual([]);
